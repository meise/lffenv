#!/bin/sh

. /etc/functions.sh

config_load tinc
config_get n "3micc" Name

MAC="`ip link show br-mesh | grep "link/ether" | sed "s/^[ ]*//" | cut -d' ' -f2`"

echo "Content-type: text/plain"
echo ""
echo "# MAC: $MAC"
[ -f "/etc/tinc/3micc/hosts/$n" ] && cat "/etc/tinc/3micc/hosts/$n"
